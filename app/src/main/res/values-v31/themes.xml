<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.MainActivity." parent="Theme.SplashScreen">
        <item name="postSplashScreenTheme">@style/Theme.ECommerce</item> <!--This is needed otherwise the app will crash-->
        <item name="android:windowSplashScreenBackground">@color/primaryColor</item>

        <item name="android:statusBarColor">@color/primaryColor</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:navigationBarColor">@color/black</item>
        <item name="android:windowLightNavigationBar">true</item>
        <item name="android:windowBackground">@drawable/ic_launcher_foreground</item>
    </style>
</resources>